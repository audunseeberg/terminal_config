# Unbind the default prefix key (Ctrl-b)
unbind C-b

# Set the new prefix key (e.g., Ctrl-a)
set-option -g prefix C-a

# Bind the new prefix key to send the prefix itself (for sending the prefix to the pane)
bind-key C-a send-prefix
